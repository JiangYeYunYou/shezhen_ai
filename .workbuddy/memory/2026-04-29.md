# 2026-04-29 工作日志

## 修复 Alembic 迁移脚本 + Docker 表结构一致性保障

### 发现的问题
- `20690fce4cff_initial_migration.py`：`upgrade()` 在删除 `user` 表（逻辑完全相反）
- `6e95eed58b47_add_diagnosis_table.py`：空的 `pass`，未创建 `diagnosis` 表
- 后续 4 个迁移均依赖不存在的 `diagnosis` 表，无法在干净数据库上运行

### 修复内容
1. **删除 6 个损坏迁移脚本**
2. **重写 `migrations/versions/000000000001_initial_migration.py`**
   - 一次性创建 `user` 和 `diagnosis` 两张表
   - 所有字段类型、约束、默认值、索引与 SQLAlchemy 模型严格对齐
   - 字符集 `utf8mb4`，引擎 `InnoDB`
3. **新建 `docker-entrypoint.sh`**
   - 启动流程：`alembic upgrade head` → `uvicorn` 启动
   - 确保 Docker 首次启动即自动创建正确表结构
4. **更新 `Dockerfile`**
   - 复制 entrypoint 脚本，使用 `ENTRYPOINT` 替代 `CMD`
   - healthcheck start-period 延长至 20s（含迁移时间）
   - **基础镜像改为 `ghcr.milu.moe/astral-sh/uv:python3.13-bookworm-slim`**（国内 ghcr.io 镜像源，uv 已预装，省去 COPY --from）
6. **更新 `DEPLOY.md`**
   - 新增「表结构一致性说明」章节，字段级对比清单
   - 新增 CentOS Stream 9 专用内容：Docker 安装命令、firewalld 防火墙配置、SELinux 排查
7. **更新 `Dockerfile` — Debian apt 源换为国内阿里云镜像**
   - builder 和 production 阶段均在 `apt-get update` 前执行 `sed` 替换源地址
   - 解决国内服务器 `deb.debian.org` 访问慢的问题
5. **更新 `docker-compose.yml`**
   - 新增 `UVICORN_WORKERS` 等环境变量
6. **更新 `DEPLOY.md`**
   - 新增「表结构一致性说明」章节，字段级对比清单
