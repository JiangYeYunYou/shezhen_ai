# 2026-04-28 工作日志

## Docker 容器化部署配置
- 新建 `Dockerfile` — 多阶段构建（builder + production），Python 3.13-slim-bookworm，uv 包管理，非 root 用户，HEALTHCHECK，4 worker Uvicorn
- 新建 `.dockerignore` — 排除开发文件、缓存、日志、敏感环境变量
- 新建 `docker-compose.yml` — MySQL 8.0 + backend 双服务，资源限制（CPU/Memory），服务健康依赖，独立网络
- 新建 `.env.example` — 环境变量模板（DB、JWT、AI 配置）
- 新建 `DEPLOY.md` — 服务器部署指南（前置要求、快速部署步骤、运维命令、安全建议、故障排查）
